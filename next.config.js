/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['img.youtube.com', 'raw.githubusercontent.com', 'encrypted-tbn0.gstatic.com'],
  },
}

module.exports = nextConfig